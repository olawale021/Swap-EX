CREATE TABLE users (
                       id INTEGER PRIMARY KEY AUTOINCREMENT,
                       username TEXT NOT NULL,
                       phone_number INTEGER NOT NULL,
                       password TEXT NOT NULL,
                       address JSON NOT NULL
);
INSERT INTO users (username, phone_number, password, address)
VALUES ('steppa55', 1234567890, 'password123', '{"street": "123 Main St", "city": "Anytown", "country": "United Kingdom", "zipcode": 12345}');

CREATE TABLE categories (
                            id INTEGER PRIMARY KEY AUTOINCREMENT,
                            name TEXT NOT NULL
);

CREATE TABLE items (
                       id INTEGER PRIMARY KEY AUTOINCREMENT,
                       user_id INTEGER NOT NULL,
                       category_id INTEGER NOT NULL,
                       description TEXT NOT NULL,
                       condition TEXT NOT NULL,
                       photos JSON,
                       created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
                       updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
                       FOREIGN KEY (user_id) REFERENCES users(id),
                       FOREIGN KEY (category_id) REFERENCES categories(id)
);
CREATE TABLE interest (
                        id INTEGER PRIMARY KEY AUTOINCREMENT,
                        item_id INTEGER NOT NULL,
                        user_id INTEGER NOT NULL,
                        interest_details JSON,
                        created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
                        FOREIGN KEY (item_id) REFERENCES items(id),
                        FOREIGN KEY (user_id) REFERENCES users(id)
);

CREATE TABLE exchanges (
                           id INTEGER PRIMARY KEY AUTOINCREMENT,
                           item_id INTEGER NOT NULL,
                           interested_user_id INTEGER NOT NULL,
                           status TEXT NOT NULL,
                           created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
                           updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
                           FOREIGN KEY (item_id) REFERENCES items(id),
                           FOREIGN KEY (interested_user_id) REFERENCES users(id)
);

CREATE TABLE messages (
                          id INTEGER PRIMARY KEY AUTOINCREMENT,
                          exchange_id INTEGER NOT NULL,
                          sender_id INTEGER NOT NULL,
                          receiver_id INTEGER NOT NULL,
                          content TEXT NOT NULL,
                          created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
                          FOREIGN KEY (exchange_id) REFERENCES exchanges(id),
                          FOREIGN KEY (sender_id) REFERENCES users(id),
                          FOREIGN KEY (receiver_id) REFERENCES users(id)
);
INSERT INTO categories (name) VALUES ('Toys');
INSERT INTO categories (name) VALUES ('Video Games & Consoles');
INSERT INTO categories (name) VALUES ('Collectables');
INSERT INTO categories (name) VALUES ('Jewellery & Watches');